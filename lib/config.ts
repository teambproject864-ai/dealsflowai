/**
 * GTM Portal Configuration
 * Single unified configuration option for all GTM-related visibility settings
 */
export const gtmPortalConfig = {
  showAllCustomerGTMContent: true, // Dedicated configuration option for GTM accessibility
};
